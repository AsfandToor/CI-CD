name: Issue Comment

on:
    issues:
        types: [opened]

jobs:
    comment-with-action:
        runs-on: ubuntu-latest
        steps:
            - name: "Dump Github Context"
              run: echo '{{ toJson(github.event) }}' || jq
              shell: bash
 